[project]
name = "github-ingest-workspace"
version = "0.1.0"
description = "GitHub ingest workspace with job decorators and deployment"
requires-python = ">=3.12"
dependencies = [
    "dlt[motherduck,workspace,hub]==1.26.0a1",
    "duckdb<1.4.3",
    "altair>=6.0.0",
]

[tool.uv.sources]

